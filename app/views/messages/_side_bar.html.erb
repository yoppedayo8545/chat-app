<div class="side-header">
    <div>
      <%= link_to current_user.name, edit_user_path(current_user) %> 
    </div>
    <div>
      <%= link_to "チャットを作成する", new_room_path %>
    </div>
  </div>
    <div class="side-main">
      <% current_user.rooms.each do |room|%>
      <div class="room">
        <div class="room_name">
          <a href='#' class="room-name"><%=room.name%></a>
        </div>
      </div>
      <% end %>
    </div>